const Colors = {
  primary: 'lightblue',
  text: 'white',
  inputBackground: 'gray',
};

const NavigationColors = {
  primary: Colors.primary,
  text: Colors.text,
};

export default {
  Colors,
  NavigationColors,
};
