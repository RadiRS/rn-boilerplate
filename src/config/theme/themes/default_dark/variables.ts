const Colors = {
  primary: 'lightblue',
  text: 'white',
  inputBackground: 'gray',
  alternative: 'black',
};

const NavigationColors = {
  primary: Colors.primary,
};

export default {
  Colors,
  NavigationColors,
};
